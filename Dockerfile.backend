FROM python:3.11-slim

WORKDIR /app

COPY pyproject.toml ./
COPY src ./src
COPY run_fastapi.py ./

RUN pip install --no-cache-dir -e .

EXPOSE 8000

CMD ["python", "-m", "uvicorn", "persona_ai.fastapi_app:app", "--host", "0.0.0.0", "--port", "8000"]
